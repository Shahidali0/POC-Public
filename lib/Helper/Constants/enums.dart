enum SnackbarType { failure, success }

enum MyAnimationType { bottomUp, rightToLeft, leftToRight, upDown }

enum AuthType { signup, login }

enum MyBookingType { upcoming, past }

enum OtpVerificationType { signup, forgotPassword }
